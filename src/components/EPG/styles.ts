import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  vertical: {
    flexDirection: 'row',
  },
  horizontal: {
    flexDirection: 'column',
  },
});
