export enum Route {
  Discover = 'route.discover',
  Movies = 'route.movies',
  Shows = 'route.shows',
  Settings = 'route.settings',
  Movie = 'route.movie',
  Show = 'route.show',
  TrendingToday = 'route.trending-today',
}
